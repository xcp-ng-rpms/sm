From 5310cf3259991ec3805cfd461fa10d3f317daf8c Mon Sep 17 00:00:00 2001
From: Damien Thenot <damien.thenot@vates.tech>
Date: Fri, 28 Mar 2025 11:03:31 +0100
Subject: [PATCH] fix(FileSR): remove a dot added at the extension

Signed-off-by: Damien Thenot <damien.thenot@vates.tech>
---
 drivers/FileSR.py | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/drivers/FileSR.py b/drivers/FileSR.py
index 2beb79a6..9306187f 100755
--- a/drivers/FileSR.py
+++ b/drivers/FileSR.py
@@ -818,7 +818,7 @@ class FileVDI(VDI.VDI):
         extension = VDI_TYPE_TO_EXTENSION[self.vdi_type]
         if snap_type == VDI.SNAPSHOT_DOUBLE:
             dest = util.gen_uuid()
-            dst = os.path.join(self.sr.path, "%s.%s" % (dest, extension))
+            dst = os.path.join(self.sr.path, "%s%s" % (dest, extension))
             args.append(dest)
 
         if self.hidden:
@@ -834,7 +834,7 @@ class FileVDI(VDI.VDI):
         newuuid = util.gen_uuid()
         src = self.path
         newsrc = os.path.join(self.sr.path, "%s%s" % (newuuid, extension))
-        newsrcname = "%s.%s" % (newuuid, extension)
+        newsrcname = "%s%s" % (newuuid, extension)
 
         if not self._checkpath(src):
             raise xs_errors.XenError('VDIUnavailable', \
